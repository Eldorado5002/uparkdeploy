{
  "name": "parking-system-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "migrate": "node -e \"require('./db').initSchema().then(()=>console.log('DB ready')).catch(e=>{console.error(e);process.exit(1)})\"",
    "client-install": "cd ../frontend && npm install",
    "client-build": "cd ../frontend && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "http": "^0.0.1-security",
    "path": "^0.12.7",
    "socket.io": "^4.8.1",
    "mqtt": "^5.13.0",
    "pg": "^8.12.0",
    "dotenv": "^16.4.5",
    "twilio": "^5.3.6"
  }
}
